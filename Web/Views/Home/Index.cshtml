@using Web.ViewModels;
@model HomeVm
@{
    ViewData["Title"] = "Entradas Engee";
}

<div id="divEntradas" class="mt-3">
    <partial name="_entrada.cshtml" for="@Model.Visita" view-data="@ViewData" ></partial>
</div>

<div id="divContent" class="mt-3">
    <partial name="_ListadoIngresos.cshtml" for="@Model.ListVisitas"></partial>
</div>

<partial name="_ModalConfirm"></partial>